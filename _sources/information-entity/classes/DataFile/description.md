# DataFile

## Overview

A digital file containing data

This class is a subclass of `cco:InformationContentEntity`.

## Usage

Used to represent instances of DataFile in CCO.

## Namespace

- **Namespace**: `https://www.commoncoreontologies.org/`
- **Suggested Prefix**: `cco:`
- **Class**: `cco:DataFile`
